const BUILD='v35-root-20260910-9-deterministic';
const ROOT=new URL('./',self.location.href);
const CACHE=`plato-root:${ROOT.pathname}:${BUILD}`;
const SHELL=new URL('index.html',ROOT).href;
const ASSETS=['index.html','manifest.webmanifest',...['history.js','era_history.js','plato_v35_phone.js','possibility_space.js','prize_coverage.js','experiment_lab.js'].map(x=>`v35/${x}?build=${BUILD}`)].map(x=>new URL(x,ROOT).href);
self.addEventListener('install',event=>event.waitUntil((async()=>{const cache=await caches.open(CACHE);await cache.addAll(ASSETS.map(url=>new Request(url,{cache:'reload'})));await self.skipWaiting()})()));
self.addEventListener('activate',event=>event.waitUntil((async()=>{for(const key of await caches.keys()){if(key===CACHE)continue;if(!key.startsWith(`plato-root:${ROOT.pathname}:`)&&!key.startsWith('oracle-plato-')&&!key.startsWith('plato-v35-'))continue;const entries=await (await caches.open(key)).keys();if(entries.every(entry=>{const u=new URL(entry.url);return u.origin===ROOT.origin&&u.pathname.startsWith(ROOT.pathname)}))await caches.delete(key)}await self.clients.claim()})()));
self.addEventListener('fetch',event=>{const request=event.request,url=new URL(request.url);if(request.method!=='GET'||url.origin!==ROOT.origin||!url.pathname.startsWith(ROOT.pathname))return;if(request.mode==='navigate'){if(url.pathname.startsWith(new URL('v35/',ROOT).pathname)){event.respondWith(Promise.resolve(Response.redirect(ROOT.href,302)));return}if(url.pathname!==ROOT.pathname&&url.pathname!==new URL('index.html',ROOT).pathname)return;event.respondWith((async()=>{try{const response=await fetch(request,{cache:'no-store'});if(!response.ok)throw new Error('Navigation unavailable');const html=await response.clone().text();if(html.includes(`content="${BUILD}"`)){const cache=await caches.open(CACHE);await cache.put(SHELL,response.clone())}return response}catch(error){const cached=await (await caches.open(CACHE)).match(SHELL);if(cached)return cached;throw error}})());return}if(!ASSETS.includes(url.href))return;event.respondWith((async()=>{const cache=await caches.open(CACHE);return await cache.match(request)||fetch(request)})())});
